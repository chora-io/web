'use client'

import styles from './Verifiers.module.css'

const Verifiers = () => {
  return (
    <div className={styles.box}>
      <div>{'no verifiers found'}</div>
    </div>
  )
}

export default Verifiers
